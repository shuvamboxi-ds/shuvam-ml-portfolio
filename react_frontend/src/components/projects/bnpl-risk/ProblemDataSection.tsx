import { bnplProject } from "../../../data/bnpl-risk/project-data"
export function ProblemDataSection() { return <section className="bnpl-section"><div className="bnpl-shell"><div className="bnpl-section-heading"><p className="bnpl-kicker">02 / THE PROBLEM + DATA REALITY</p><h2>Accuracy alone would lie here.</h2><p>The target distribution is strongly imbalanced.</p></div><div className="bnpl-problem-grid"><div className="bnpl-distribution"><div className="bnpl-bars">{bnplProject.targetDistribution.map((item) => <div key={item.label}><div className="bnpl-bar-label"><strong>{item.label.toUpperCase()}</strong><span>{item.count.toLocaleString()} · {item.percentage}%</span></div><i><b className={item.label === "High" ? "is-risk" : ""} style={{ width: `${item.percentage}%` }} /></i></div>)}</div><div className="bnpl-method-callout"><span>MULTICLASS RISK CLASSIFICATION</span><p>Although the dataset is described as default risk, the actual target contains Low, Medium and High classes. Therefore the project was treated as multiclass risk classification rather than forcing an artificial binary default label.</p></div></div><div className="bnpl-snapshot"><p>DATA SNAPSHOT</p>{[["ROWS",bnplProject.rows],["ORIGINAL COLUMNS",bnplProject.originalColumns],["MISSING INCOME",bnplProject.missingIncome],["MISSING CREDIT SCORE",bnplProject.missingCreditScore],["TARGET CLASSES",bnplProject.targetClasses]].map(([label,value]) => <div key={label}><span>{label}</span><strong>{value}</strong></div>)}</div></div></div></section> }
