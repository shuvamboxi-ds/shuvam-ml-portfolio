import { bnplProject } from "../../../data/bnpl-risk/project-data"
export function ErrorAnalysisSection() { const { labels, values } = bnplProject.confusionMatrix; return <section className="bnpl-section"><div className="bnpl-shell"><div className="bnpl-section-heading"><p className="bnpl-kicker">05 / WHAT THE MODEL ACTUALLY GOT WRONG</p><h2>96.5% accuracy isn't the interesting part.</h2></div><div className="bnpl-error-grid"><div className="bnpl-confusion"><div className="bnpl-panel-top"><span>CONFUSION MATRIX · XGBOOST</span><span>PREDICTED →</span></div><table><thead><tr><th>ACTUAL</th>{labels.map(label => <th key={label}>{label.toUpperCase()}</th>)}</tr></thead><tbody>{values.map((row,index) => <tr key={labels[index]}><th>{labels[index].toUpperCase()}</th>{row.map((value,column) => <td className={index === 2 && column === 0 ? "is-danger" : index !== column ? "is-boundary" : ""} key={`${index}-${column}`}>{value}</td>)}</tr>)}</tbody></table><p><strong>ONLY 1</strong> actual High-risk customer was classified directly as Low.</p></div><div className="bnpl-roc"><span>MACRO OVR ROC-AUC</span><strong>0.991</strong><p>This measures class-separation ability.</p><div>It does <b>not</b> mean 99.1% accuracy or 99.1% calibrated default probability.</div><h3>MEDIUM RISK<br />was the hardest class to separate.</h3><p>Medium customers sit between clearer Low and High patterns, making them the most ambiguous group.</p></div></div></div></section> }
