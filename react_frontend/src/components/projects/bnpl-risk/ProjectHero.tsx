import { ArrowRight, ExternalLink } from "lucide-react"
import { bnplProject } from "../../../data/bnpl-risk/project-data"

function RiskFlow() { return <div className="bnpl-risk-visual"><div className="bnpl-visual-top"><span>RISK CLASSIFIER / XGBOOST</span><span>COMPLETED</span></div><div className="bnpl-flow">{["CUSTOMER SIGNALS", "FEATURE PIPELINE", "XGBOOST", "LOW / MEDIUM / HIGH"].map((item, index) => <div key={item}><span>{String(index + 1).padStart(2, "0")}</span><strong>{item}</strong>{index < 3 && <i>→</i>}</div>)}</div><div className="bnpl-signal-grid"><span>INCOME_USD</span><span>CREDIT_SCORE</span><span>BNPL_DEBT</span><span>LATE_PAYMENTS</span><span>ACTIVE_LOANS</span><span>EMPLOYMENT</span></div><div className="bnpl-output"><span>SELECTED OUTPUT</span><strong>EXPLAINABLE RISK BANDS</strong></div></div> }
export function ProjectHero() { return <section className="bnpl-section bnpl-hero"><div className="bnpl-shell bnpl-hero-grid"><div><p className="bnpl-kicker">01 / MACHINE LEARNING · FINTECH · MULTICLASS CLASSIFICATION</p><h1>BNPL Default<br />Risk Prediction</h1><p className="bnpl-hero-headline">Predicting customer risk before BNPL exposure becomes dangerous.</p><p className="bnpl-hero-copy">An end-to-end machine learning system that classifies Buy Now, Pay Later customers into Low, Medium, or High risk using financial and behavioral signals.</p><div className="bnpl-hero-meta"><span>ROLE <b>Data Science / Machine Learning</b></span><span>PROBLEM <b>Multiclass Risk Classification</b></span><span>DATA <b>10,000 Customers</b></span><span>FINAL MODEL <b>XGBoost</b></span></div><div className="bnpl-actions"><a className="bnpl-button bnpl-primary" href={bnplProject.demoUrl}>VIEW LIVE DEMO <ArrowRight size={16} /></a><a className="bnpl-button bnpl-secondary" href={bnplProject.sourceUrl} target="_blank" rel="noopener noreferrer">VIEW SOURCE <ExternalLink size={15} /></a></div></div><RiskFlow /></div><div className="bnpl-shell bnpl-metrics">{bnplProject.headlineMetrics.map(([value,label], index) => <div key={label}><strong className={index > 0 ? "is-amber" : ""}>{value}</strong><span>{label}</span></div>)}</div></section> }
