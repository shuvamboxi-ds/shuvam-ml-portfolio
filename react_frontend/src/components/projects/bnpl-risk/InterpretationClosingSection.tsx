import { ArrowRight, ExternalLink } from "lucide-react"
import { bnplProject } from "../../../data/bnpl-risk/project-data"
export function InterpretationClosingSection() { return <section className="bnpl-section bnpl-closing"><div className="bnpl-shell"><div className="bnpl-section-heading"><p className="bnpl-kicker">06 / INTERPRETATION + LIMITATIONS + OUTCOME</p><h2>What drove the model?</h2></div><div className="bnpl-importance-list">{bnplProject.featureImportance.map((item,index) => <div key={item.feature}><div><span>{item.feature}</span><strong>{item.importance}%</strong></div><i><b className={index === 0 ? "is-amber" : ""} style={{ width: `${item.importance}%` }} /></i></div>)}</div><p className="bnpl-footnote">Feature importance represents predictive usefulness, not causation.</p><div className="bnpl-system"><div><p className="bnpl-kicker">FROM NOTEBOOK TO USABLE SYSTEM</p><div>{["USER INPUT", "FEATURE PREPARATION", "SAVED PIPELINE", "XGBOOST", "CLASS PROBABILITIES"].map((item,index) => <span key={item}>{item}{index < 4 && " → "}</span>)}</div></div><aside><p>ARTIFACTS</p>{bnplProject.artifacts.map(artifact => <code key={artifact}>{artifact}</code>)}</aside></div><div className="bnpl-limitations"><p className="bnpl-kicker">WHAT I WOULD FIX NEXT</p>{bnplProject.limitations.map((item,index) => <div key={item}><span>0{index+1}</span><strong>{item}</strong></div>)}</div><div className="bnpl-final"><p>The goal wasn't to chase the highest accuracy.</p><h3>It was to build a risk classifier whose decisions, trade-offs and limitations could be explained.</h3><div className="bnpl-final-meta"><span>XGBOOST</span><span>96.5% ACCURACY</span><span>0.876 MACRO F1</span><span>0.991 ROC-AUC</span></div><div className="bnpl-actions"><a className="bnpl-button bnpl-primary" href={bnplProject.demoUrl}>TRY LIVE MODEL <ArrowRight size={16} /></a><a className="bnpl-button bnpl-secondary" href={bnplProject.sourceUrl} target="_blank" rel="noopener noreferrer">VIEW GITHUB <ExternalLink size={15} /></a><a className="bnpl-back" href="/">BACK TO PROJECTS</a></div></div></div></section> }
