import { ArrowLeft, ExternalLink } from "lucide-react"
import { bnplDemoConfig } from "../../../../data/bnpl-risk/demo-config"
export function DemoHeader() { return <header className="bnpl-demo-header"><div className="bnpl-demo-nav"><a href={bnplDemoConfig.caseStudyUrl}><ArrowLeft size={15} /> BACK TO BNPL CASE STUDY</a><a href={bnplDemoConfig.sourceUrl} target="_blank" rel="noopener noreferrer">GITHUB <ExternalLink size={13} /></a></div><p className="bnpl-demo-kicker">LIVE MODEL / BNPL RISK CLASSIFIER</p><h1>Test the risk model.</h1><p className="bnpl-demo-description">Enter a customer profile and run it through the trained XGBoost classification pipeline.</p><div className="bnpl-demo-meta"><span>MODEL <b>XGBoost</b></span><span>OUTPUT <b>Low / Medium / High</b></span><span>INPUTS <b>9 Customer Fields</b></span><span>STATUS <b>● Model Ready</b></span></div></header> }
