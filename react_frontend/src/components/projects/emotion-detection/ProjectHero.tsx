import { ArrowRight, ExternalLink } from "lucide-react"
import { emotionProject } from "../../../data/emotion-detection/project-data"

function ClassificationVisual() { const labels = ["ANGER", "FEAR", "JOY", "LOVE", "SAD", "SURPRISE"]; return <div className="emotion-hero-visual"><div className="emotion-visual-top"><span>TEXT → EMOTION</span><span>SIX CLASS OUTPUT</span></div><div className="emotion-sentence">[ ILLUSTRATIVE TEXT INPUT ]</div><div className="emotion-token-row">{["The", "signal", "finally", "made", "sense", "after", "weeks"].map((token, index) => <span className={index === 4 ? "is-focus" : ""} key={token}>{token}</span>)}</div><div className="emotion-labels">{labels.map((label, index) => <div key={label}><span>{label}</span><i><b style={{ width: `${[22, 12, 78, 42, 26, 56][index]}%` }} /></i></div>)}</div><div className="emotion-visual-result"><span>PREDICTED EMOTION</span><strong>JOY</strong><em>DistilBERT classification head</em></div></div> }

export function ProjectHero() { return <section className="emotion-section emotion-hero"><div className="emotion-shell emotion-hero-grid"><div><p className="emotion-kicker">03 / NLP + TRANSFORMERS</p><h1>Emotion Detection<br />from Text</h1><p className="emotion-lede">A six-class NLP classification system comparing traditional TF-IDF features with a fine-tuned DistilBERT model.</p><div className="emotion-tags">{emotionProject.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><div className="emotion-actions"><a className="emotion-button emotion-button-primary" href={emotionProject.demoUrl}>LIVE DEMO <ArrowRight size={16} /></a><a className="emotion-button emotion-button-secondary" href={emotionProject.sourceUrl} target="_blank" rel="noopener noreferrer">VIEW SOURCE <ExternalLink size={15} /></a></div></div><ClassificationVisual /></div><div className="emotion-shell emotion-metric-strip">{emotionProject.heroMetrics.map(({ label, value }) => <div key={label}><strong className={value === "99.07%" ? "is-amber" : ""}>{label}</strong><span>{value}</span></div>)}</div></section> }
