import type { FormEvent } from "react"

interface EmotionInputProps { value: string; disabled: boolean; validationMessage?: string; onChange: (value: string) => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void }
export function EmotionInput({ value, disabled, validationMessage, onChange, onSubmit }: EmotionInputProps) { return <form className="emotion-input" onSubmit={onSubmit}><label htmlFor="emotion-text">INPUT TEXT</label><textarea id="emotion-text" value={value} onChange={(event) => onChange(event.target.value)} placeholder={'"I finally got the opportunity I\'ve been waiting for."'} disabled={disabled} aria-describedby={validationMessage ? "emotion-validation" : "emotion-limit"} /><div className="emotion-input-footer"><span id="emotion-limit">MODEL LIMIT · 64 TOKENS</span><button type="submit" disabled={disabled}>{disabled ? "ANALYZING..." : "ANALYZE EMOTION →"}</button></div>{validationMessage && <p id="emotion-validation" className="emotion-inline-error" role="alert">{validationMessage}</p>}</form> }
