import { emotionDemoConfig } from "../../../../data/emotion-detection/demo-config"
import { ModelDetails } from "./ModelDetails"

export function ModelContext() { return <section className="emotion-context"><div className="emotion-context-strip">{[["MODEL", emotionDemoConfig.displayModel], ["TASK", emotionDemoConfig.task], ["TEST MACRO F1", emotionDemoConfig.testMacroF1], ["MAX LENGTH", emotionDemoConfig.maxLength], ["TRAINING", emotionDemoConfig.training]] .map(([label, value]) => <div key={label}><span>{label}</span><strong className={label === "TEST MACRO F1" ? "is-amber" : ""}>{value}</strong></div>)}</div><div className="emotion-limitation"><p>MODEL LIMITATION</p><strong>This classifier predicts one dominant emotion from six learned categories.</strong><span>Real text can contain multiple emotions, ambiguity, sarcasm or language outside the training distribution. Confidence is a model score and should not be interpreted as guaranteed correctness.</span></div><ModelDetails /></section> }
