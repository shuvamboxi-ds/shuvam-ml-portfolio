import { emotionProject } from "../../../data/emotion-detection/project-data"

export function ProblemDataSection() { return <section className="emotion-section emotion-problem"><div className="emotion-shell"><div className="emotion-section-heading"><p className="emotion-kicker">02 / PROBLEM + DATA REALITY</p><h2>Before the model, the labels had to agree.</h2></div><div className="emotion-problem-grid"><article className="emotion-problem-copy"><span>THE PROBLEM</span><h3>Given a sentence, classify its dominant emotion into one of six labels.</h3><p>Single-label multiclass text classification.</p><div className="emotion-class-list">{emotionProject.classesList.map((label) => <span key={label}>{label}</span>)}</div></article><article className="emotion-audit"><div className="emotion-audit-flow"><strong>{emotionProject.rawRows}<small>RAW ROWS</small></strong><span>↓ duplicates + conflicting labels removed ↓</span><strong className="is-clean">{emotionProject.cleanRows}<small>FINAL CLEAN DATA</small></strong></div><div className="emotion-audit-facts"><div><strong>{emotionProject.conflictSentences}</strong><span>sentences had conflicting labels</span></div><div><strong>{emotionProject.conflictRows}</strong><span>rows belonged to conflicting sentences</span></div><div><strong>{emotionProject.conflictPercentage}</strong><span>of raw data was affected</span></div></div><p>Conflicting examples were removed before splitting to avoid contradictory supervision and exact sentence leakage.</p></article></div></div></section> }
