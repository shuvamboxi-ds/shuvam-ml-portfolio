import { emotionProject } from "../../../data/emotion-detection/project-data"

export function FailureAnalysisSection() { const max = emotionProject.confusionPairs[0].count; const surprise = emotionProject.classMetrics[0]; return <section className="emotion-section emotion-failure"><div className="emotion-shell"><div className="emotion-section-heading"><p className="emotion-kicker">05 / FAILURE ANALYSIS</p><h2>99.31% ACCURACY<br />DOESN'T MEAN PERFECT UNDERSTANDING.</h2></div><div className="emotion-failure-grid"><article className="emotion-confusions"><div className="emotion-panel-top"><span>TOP CONFUSION PAIRS</span><span>COUNT</span></div>{emotionProject.confusionPairs.map(({ trueLabel, predictedLabel, count }) => <div className="emotion-confusion-row" key={`${trueLabel}-${predictedLabel}`}><strong>{trueLabel} → {predictedLabel}</strong><i><b style={{ width: `${count / max * 100}%` }} /></i><span>{count}</span></div>)}</article><article className="emotion-surprise"><span>SURPRISE</span><strong>{surprise.precision.toFixed(2)}%</strong><small>PRECISION</small><strong>{surprise.recall.toFixed(2)}%</strong><small>RECALL</small><p>The model is highly precise when predicting surprise, but some genuine surprise examples are classified as fear.</p></article></div><div className="emotion-errors"><div><span>ERROR ANALYSIS</span><strong>REAL ERROR EXAMPLES TO BE ADDED FROM EVALUATION OUTPUT</strong><p>No representative text examples are available in the current repository. This placeholder prevents fabricated dataset observations from being presented as real.</p></div><div className="emotion-error-types"><span>REMAINING ERROR TYPES</span><b>semantic overlap</b><b>ambiguous labels</b><b>insufficient context</b><b>multiple plausible emotions</b><b>high-confidence mistakes</b></div></div><div className="emotion-confidence">HIGH CONFIDENCE <strong>≠</strong> GUARANTEED CORRECTNESS</div></div></section> }
