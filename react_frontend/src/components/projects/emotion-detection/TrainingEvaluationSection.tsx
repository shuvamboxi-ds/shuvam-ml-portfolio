import { emotionProject, experimentMetrics } from "../../../data/emotion-detection/project-data"

const config = emotionProject.trainingConfig
export function TrainingEvaluationSection() { return <section className="emotion-section emotion-training"><div className="emotion-shell"><div className="emotion-section-heading"><p className="emotion-kicker">04 / TRAINING + EVALUATION</p><h2>Context helped. The comparison made it legible.</h2></div><div className="emotion-config">{config.map(([label, value]) => <div key={label}><span>{label}</span><strong>{value}</strong></div>)}</div><div className="emotion-compare"><div><span>TF-IDF + LOGISTIC REGRESSION</span><strong>{emotionProject.baselineMacroF1}</strong><small>MACRO F1</small></div><div className="emotion-compare-arrow">→</div><div className="is-final"><span>DISTILBERT</span><strong>{emotionProject.distilbertTestMacroF1}</strong><small>MACRO F1</small></div><b className="emotion-improvement">+2.99 percentage points</b></div><div className="emotion-test-results">{experimentMetrics.test.map(([label, value]) => <div key={label}><span>{label}</span><strong className={label === "MACRO F1" ? "is-amber" : ""}>{value}</strong></div>)}</div><p className="emotion-eval-note">Final test metrics are reported on the cleaned held-out distribution. They are not a guarantee of real-world emotion understanding.</p></div></section> }
