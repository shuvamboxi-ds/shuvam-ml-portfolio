import { ArrowRight } from "lucide-react"
import { delhiDemandProject } from "../../../data/delhi-electricity-demand-forecasting/project-data"

const pipeline = ["RAW LOAD", "HOURLY SERIES", "LAG FEATURES", "ROLLING HISTORY", "CALENDAR FEATURES", "MODEL"]

export function FeatureEngineeringSection() { return <section className="delhi-section delhi-features"><div className="delhi-shell"><div className="delhi-section-heading"><p className="delhi-kicker">03 / FEATURE ENGINEERING</p><h2>Turning History Into Predictive Signals</h2></div><div className="delhi-pipeline">{pipeline.map((step, index) => <div key={step}><span>{String(index + 1).padStart(2, "0")}</span><strong>{step}</strong>{index < pipeline.length - 1 && <ArrowRight aria-hidden="true" size={15} />}</div>)}</div><div className="delhi-feature-grid"><article className="delhi-feature-list"><div className="delhi-subhead"><span>FINAL 8 FEATURES</span><span>4 TEMPORAL / 4 HISTORICAL LOAD</span></div><div className="feature-items">{delhiDemandProject.features.map(([feature, description]) => <div key={feature}><strong>{feature}</strong><span>{description}</span></div>)}</div></article><article className="delhi-correlation"><span>HISTORICAL SIGNAL</span>{delhiDemandProject.correlations.map(([feature, value]) => <div key={feature}><strong>{feature}</strong><i><b style={{ width: `${Number(value) * 100}%` }} /></i><span>{value}</span></div>)}<p>Correlation is evidence of repeated structure, not proof of causation.</p></article></div><article className="delhi-leakage"><div><span>LEAKAGE CHECK</span><strong>t-24 ... t-2 → t-1 → <em>[ FORECAST t ]</em></strong></div><p>Rolling features were constructed from historical observations only. The current target demand was never allowed to enter its own feature values.</p></article></div></section> }
