import type { HistoricalTimestampOption } from "../../../../types/delhi-electricity-demand-demo"

type Props = { timestamps: HistoricalTimestampOption[]; selected: string; onSelect: (value: string) => void; onRun: () => void; loading: boolean; unavailable: boolean }
export function HistoricalDemoPanel({ timestamps, selected, onSelect, onRun, loading, unavailable }: Props) { return <div className="forecast-mode-panel"><span className="forecast-panel-label">HISTORICAL TEST SIMULATION</span><p>Run the trained model against a timestamp from the untouched future test period.</p>{unavailable ? <div className="forecast-inline-warning"><strong>FORECAST SERVICE UNAVAILABLE</strong><span>Connect the configured API to load valid historical timestamps.</span></div> : <><label className="forecast-select-label" htmlFor="forecast-timestamp">SELECT TIMESTAMP</label><select id="forecast-timestamp" value={selected} onChange={(event) => onSelect(event.target.value)}><option value="">Choose a valid test timestamp</option>{timestamps.map((option) => <option key={option.timestamp} value={option.timestamp}>{option.label}</option>)}</select></>}<button type="button" className="forecast-run" onClick={onRun} disabled={loading || !selected}>{loading ? "GENERATING FORECAST..." : "GENERATE FORECAST →"}</button></div> }
