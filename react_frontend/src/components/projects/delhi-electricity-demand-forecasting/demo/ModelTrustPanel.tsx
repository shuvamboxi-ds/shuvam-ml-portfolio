import { ArrowLeft, ExternalLink } from "lucide-react"
import { forecastDemoConfig } from "../../../../data/delhi-electricity-demand-forecasting/demo-config"
import { delhiDemandProject } from "../../../../data/delhi-electricity-demand-forecasting/project-data"

export function ModelTrustPanel() { return <section className="model-trust"><div className="forecast-section-title"><span>04 / MODEL TRUST</span><h2>How reliable was the model?</h2></div><div className="trust-layout"><div><span className="forecast-panel-label">UNTOUCHED FUTURE TEST SET</span><div className="trust-metrics">{forecastDemoConfig.metrics.map(([label, value]) => <div key={label}><strong>{value}</strong><span>{label}</span></div>)}</div><div className="trust-period"><span>TEST PERIOD</span><strong>{forecastDemoConfig.testPeriod}</strong></div></div><div className="trust-copy"><p>The final model was selected using an earlier validation period, retrained using training + validation history, and evaluated once on the latest held-out test period.</p><p>Live forecasts may perform differently from historical test results because electricity-demand patterns can change over time.</p></div></div><div className="trust-actions"><a href="/projects/delhi-electricity-demand-forecasting"><ArrowLeft size={15} /> BACK TO CASE STUDY</a>{delhiDemandProject.sourceUrl ? <a href={delhiDemandProject.sourceUrl} target="_blank" rel="noopener noreferrer">VIEW SOURCE CODE <ExternalLink size={14} /></a> : <span>VIEW SOURCE CODE <small>URL NOT CONFIGURED</small></span>}</div></section> }
