import { delhiDemandProject } from "../../../data/delhi-electricity-demand-forecasting/project-data"

export function ValidationModelSection() { return <section className="delhi-section delhi-validation"><div className="delhi-shell"><div className="delhi-section-heading"><p className="delhi-kicker">04 / VALIDATION + MODEL SELECTION</p><h2>Forecasting the Future Means Training on the Past</h2><p>Random train/test splitting was intentionally avoided. The data remained chronological.</p></div><div className="delhi-timeline">{delhiDemandProject.splits.map(([label, rows, dates, description], index) => <div className={`timeline-step timeline-step--${label.toLowerCase()}`} key={label}><div className="timeline-dot" /><span>{label}</span><strong>{rows}</strong><small>{dates}</small><p>{description}</p>{index < 2 && <i />}</div>)}</div><div className="delhi-model-layout"><div className="delhi-model-table"><div className="delhi-table-head"><span>VALIDATION RESULTS</span><span>MAE / RMSE</span></div>{delhiDemandProject.validationModels.map((model) => <div className={`delhi-model-row ${model.name === "Gradient Boosting v2" ? "is-selected" : ""}`} key={model.name}><strong>{model.name}</strong><span>{model.mae.toFixed(2)} MW</span><span>{model.rmse.toFixed(2)} MW</span></div>)}</div><aside className="delhi-selected-model"><span>SELECTED MODEL</span><h3>Gradient Boosting v2</h3><strong>HistGradientBoostingRegressor</strong><code>max_iter = 200<br />learning_rate = 0.05</code><p>Persistence established the baseline. Linear Regression tested mostly linear relationships, Random Forest tested nonlinear interactions, and Gradient Boosting improved by sequentially correcting residual errors.</p></aside></div></div></section> }
