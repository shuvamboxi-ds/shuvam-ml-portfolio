import { ArrowRight, ExternalLink } from "lucide-react"
import { cropDiseaseProject } from "../../../data/crop-disease/project-data"
export function ProjectClosing() { return <section className="crop-section crop-closing"><div className="crop-shell"><p className="crop-kicker">06 / LIVE MODEL / FINAL CTA</p><h2>See what the model does with an image.</h2><p>Upload a plant leaf image and inspect the predicted crop, condition, confidence score, and confidence-gate behavior.</p><div className="crop-actions"><a className="crop-button crop-primary" href={cropDiseaseProject.demoUrl}>LAUNCH LIVE MODEL <ArrowRight size={16} /></a><a className="crop-button crop-secondary" href={cropDiseaseProject.githubUrl} target="_blank" rel="noopener noreferrer">VIEW SOURCE ON GITHUB <ExternalLink size={15} /></a></div><div className="crop-closing-meta">{["RESNET50", "PYTORCH", "STREAMLIT", "38 CLASSES", "90% CONFIDENCE GATE"].map(item => <span key={item}>{item}</span>)}</div></div></section> }
