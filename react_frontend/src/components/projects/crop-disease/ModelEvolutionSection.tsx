import { cropDiseaseProject } from "../../../data/crop-disease/project-data"
export function ModelEvolutionSection() { return <section className="crop-section"><div className="crop-shell"><div className="crop-section-heading"><p className="crop-kicker">03 / MODEL EVOLUTION</p><h2>The model was improved deliberately — not just made larger.</h2></div><div className="crop-stages">{cropDiseaseProject.stages.map((stage,index) => <article key={stage.name}><div className="crop-stage-top"><span>0{index+1}</span><strong>{stage.name}</strong></div><div className="crop-stage-metrics"><div><b>{stage.accuracy}%</b><span>VALIDATION ACCURACY</span></div><div><b>{stage.macroF1.toFixed(4)}</b><span>MACRO F1</span></div></div><p>{stage.description}</p>{index < 2 && <i className="crop-stage-arrow">↓</i>}</article>)}</div><div className="crop-evolution-note">PROGRESSIVE FINE-TUNING PRESERVED USEFUL PRETRAINED REPRESENTATIONS WHILE ADAPTING HIGH-LEVEL VISUAL FEATURES TO PLANT DISEASE PATTERNS.</div></div></section> }
