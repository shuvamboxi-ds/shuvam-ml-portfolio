import { ArrowRight, ExternalLink } from "lucide-react"
import { cropDiseaseProject } from "../../../data/crop-disease/project-data"
function LeafVisual() { return <div className="crop-hero-visual"><div className="crop-visual-label"><span>VISION SYSTEM / RESNET50</span><span>COMPLETED / DEMO IN PROGRESS</span></div><div className="crop-leaf-placeholder"><div className="crop-leaf-shape" /><span>PROJECT VISUAL PLACEHOLDER</span></div><div className="crop-visual-annotations"><span>INPUT · LEAF IMAGE</span><span>FEATURES · VISUAL CUES</span><strong>→ DISEASE RESULT</strong></div></div> }
export function ProjectHero() { return <section className="crop-section crop-hero"><div className="crop-shell crop-hero-grid"><div><p className="crop-kicker">COMPUTER VISION / DEEP LEARNING</p><h1>Crop Disease<br />Detector</h1><p className="crop-hero-copy">A confidence-aware plant disease classifier built with PyTorch and fine-tuned ResNet50 across 38 crop conditions.</p><div className="crop-tags">{["PYTORCH", "RESNET50", "38 CLASSES", "54,303 IMAGES", "99.42% TEST ACCURACY"].map(tag => <span key={tag}>{tag}</span>)}</div><div className="crop-actions"><a className="crop-button crop-primary" href={cropDiseaseProject.demoUrl}>LAUNCH LIVE MODEL <ArrowRight size={16} /></a><a className="crop-button crop-secondary" href={cropDiseaseProject.githubUrl} target="_blank" rel="noopener noreferrer">VIEW GITHUB <ExternalLink size={15} /></a></div></div><LeafVisual /></div><div className="crop-shell crop-metrics">{cropDiseaseProject.headlineMetrics.map(item => <div key={item.label}><strong className={item.label === "MACRO F1" ? "is-amber" : ""}>{item.value}</strong><span>{item.label}</span></div>)}</div></section> }
