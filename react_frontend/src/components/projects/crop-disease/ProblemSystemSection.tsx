import { cropDiseaseProject } from "../../../data/crop-disease/project-data"
export function ProblemSystemSection() { const steps = ["LEAF IMAGE", "PREPROCESSING", "FINE-TUNED RESNET50", "SOFTMAX", "90% CONFIDENCE GATE", "DISEASE RESULT"]; return <section className="crop-section"><div className="crop-shell"><div className="crop-section-heading"><p className="crop-kicker">02 / THE PROBLEM + SYSTEM</p><h2>From leaf image to confidence-aware diagnosis.</h2><p>Plant disease classification can work extremely well on controlled image datasets, but raw classification accuracy alone is not enough.</p></div><div className="crop-pipeline">{steps.map((step,index) => <div key={step} className={step.includes("GATE") ? "is-gate" : ""}><span>{String(index+1).padStart(2,"0")}</span><strong>{step}</strong>{index < steps.length - 1 && <i>→</i>}</div>)}</div><div className="crop-system-copy"><p>MODEL COMPARISON · CONFIDENCE-AWARE INFERENCE · EXPLAINABILITY · DEPLOYMENT BEHAVIOR</p><div className="crop-dataset">{[[cropDiseaseProject.datasetSize,"IMAGES"],[cropDiseaseProject.numClasses,"CLASSES"],[cropDiseaseProject.trainSize,"TRAIN"],[cropDiseaseProject.validationSize,"VALIDATION"],[cropDiseaseProject.testSize,"TEST"]].map(([value,label]) => <div key={label}><strong>{value}</strong><span>{label}</span></div>)}</div><span className="crop-note">STRATIFIED SPLIT · FINAL TEST SET REMAINED UNTOUCHED DURING MODEL SELECTION</span></div></div></section> }
