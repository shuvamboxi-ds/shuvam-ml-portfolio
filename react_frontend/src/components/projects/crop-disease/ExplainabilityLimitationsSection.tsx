import { cropDiseaseProject } from "../../../data/crop-disease/project-data"
export function ExplainabilityLimitationsSection() { return <section className="crop-section"><div className="crop-shell"><div className="crop-section-heading"><p className="crop-kicker">05 / EXPLAINABILITY + LIMITATIONS</p><h2>99.42% accuracy was not the end of the investigation.</h2></div><div className="crop-gradcam"><div className="crop-gradcam-placeholder"><span>GRAD-CAM VISUALS</span><strong>REAL ASSETS TO BE SUPPLIED</strong><small>No verified Grad-CAM image assets are available in the current project folder. This placeholder avoids fabricating model explanations.</small></div><div><p>EXPLAINABILITY FINDINGS</p><ul><li>Some predictions focused meaningfully on leaf regions.</li><li>Others also responded strongly to leaf boundaries and image backgrounds.</li></ul><div className="crop-limit-tags">{cropDiseaseProject.limitations.map(item => <span key={item}>{item}</span>)}</div></div></div><div className="crop-field-limit"><div><p>FIELD-DATA LIMITATION</p><strong>PlantVillage performance should not be interpreted as equivalent performance on uncontrolled field images.</strong></div><div className="crop-field-signals">{cropDiseaseProject.fieldSignals.map(item => <span key={item}>{item}</span>)}</div></div></div></section> }
