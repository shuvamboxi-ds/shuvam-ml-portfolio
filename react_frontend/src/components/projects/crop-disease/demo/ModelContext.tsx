import { ArrowRight } from "lucide-react"
import { cropDiseaseDemoConfig } from "../../../../data/crop-disease/demo-config"
export function ModelContext() { return <section className="crop-context"><div className="crop-context-metrics"><div><strong>{cropDiseaseDemoConfig.testAccuracy}</strong><span>TEST ACCURACY</span></div><div><strong>{cropDiseaseDemoConfig.macroF1}</strong><span>MACRO F1</span></div><div><strong>{cropDiseaseDemoConfig.testSet}</strong><span>TEST SET</span></div></div><div className="crop-context-bottom"><p>MODEL CONTEXT</p><span>This model was trained and evaluated on PlantVillage, which contains relatively controlled leaf images. Predictions on real-world field photographs may be less reliable and should not be treated as professional agricultural diagnosis.</span><a href={cropDiseaseDemoConfig.caseStudyUrl}>READ FULL CASE STUDY <ArrowRight size={14} /></a></div></section> }
