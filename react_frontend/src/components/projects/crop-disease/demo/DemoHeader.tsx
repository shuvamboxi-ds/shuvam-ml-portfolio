import { ArrowLeft, ExternalLink } from "lucide-react"
import { cropDiseaseDemoConfig } from "../../../../data/crop-disease/demo-config"
export function DemoHeader() { const available = Boolean(import.meta.env.VITE_CROP_DISEASE_API_URL); return <header className="crop-demo-header"><div className="crop-demo-nav"><a href={cropDiseaseDemoConfig.caseStudyUrl}><ArrowLeft size={15} /> BACK TO CASE STUDY</a><a href={cropDiseaseDemoConfig.githubUrl} target="_blank" rel="noopener noreferrer">GITHUB <ExternalLink size={13} /></a></div><p className="crop-demo-kicker">LIVE MODEL / COMPUTER VISION</p><h1>Crop Disease Detector</h1><p className="crop-demo-description">Upload a leaf image and run it through the fine-tuned ResNet50 classifier.</p><div className="crop-demo-meta"><span>MODEL <b>ResNet50</b></span><span>CLASSES <b>38</b></span><span>CONFIDENCE GATE <b>90%</b></span><span>MODEL STATUS <b className={!available ? "is-unavailable" : ""}>● {available ? "READY" : "UNAVAILABLE"}</b></span></div></header> }
