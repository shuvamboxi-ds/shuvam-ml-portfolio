import type { CropDiseasePredictionResponse } from "../../types/crop-disease-demo"
export async function predictCropDisease(file: File): Promise<CropDiseasePredictionResponse> { const endpoint = import.meta.env.VITE_CROP_DISEASE_API_URL; if (!endpoint) throw new Error("MODEL_SERVICE_UNAVAILABLE"); const form = new FormData(); form.append("file", file); const response = await fetch(`${endpoint.replace(/\/$/, "")}/predict`, { method: "POST", body: form }); if (!response.ok) throw new Error(response.status === 503 ? "MODEL_UNAVAILABLE" : "INFERENCE_FAILED"); const payload = await response.json() as CropDiseasePredictionResponse; if (typeof payload.prediction_available !== "boolean") throw new Error("INFERENCE_FAILED"); return payload }
