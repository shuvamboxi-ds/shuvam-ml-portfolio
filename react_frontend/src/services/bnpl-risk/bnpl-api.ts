import type { BNPLCustomerInput, BNPLPredictionResponse, RiskClass } from "../../types/bnpl-risk-demo"
const risks = ["Low", "Medium", "High"] as RiskClass[]
export async function predictBNPLRisk(customer: BNPLCustomerInput): Promise<BNPLPredictionResponse> { const endpoint = import.meta.env.VITE_BNPL_API_URL; if (!endpoint) throw new Error("MODEL_SERVICE_UNAVAILABLE"); const response = await fetch(`${endpoint.replace(/\/$/, "")}/predict`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(customer) }); if (!response.ok) throw new Error("MODEL_INFERENCE_FAILED"); const payload = await response.json() as Partial<BNPLPredictionResponse>; if (!risks.includes(payload.predicted_risk as RiskClass) || !payload.probabilities || typeof payload.debt_to_income_ratio !== "number") throw new Error("MODEL_INFERENCE_FAILED"); return payload as BNPLPredictionResponse }
